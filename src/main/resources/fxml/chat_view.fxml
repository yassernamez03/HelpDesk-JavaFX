<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.helpdesk.controller.ChatController"
            prefHeight="650.0" prefWidth="380.0"
            styleClass="mobile-container">

    <!-- Header -->
    <top>
        <HBox styleClass="header">
            <Label text="IT Support Helpdesk" styleClass="header-text"/>
            <Pane HBox.hgrow="ALWAYS" />
            <Button fx:id="settingsButton" text="⚙" styleClass="icon-button" onAction="#openSettings"/>
        </HBox>
    </top>

    <!-- Chat area -->
    <center>
        <ScrollPane fx:id="chatScrollPane" fitToWidth="true" styleClass="chat-area">
            <VBox fx:id="chatContainer" spacing="10">
                <padding>
                    <Insets top="10" right="10" bottom="10" left="10"/>
                </padding>
            </VBox>
        </ScrollPane>
    </center>

    <!-- Input area -->
    <bottom>
        <HBox spacing="5" styleClass="input-area">
            <padding>
                <Insets top="10" right="10" bottom="10" left="10"/>
            </padding>
            <TextField fx:id="messageInput" promptText="Ask me anything about IT..."
                       HBox.hgrow="ALWAYS" onAction="#sendMessage"/>
            <Button text="Send" onAction="#sendMessage" styleClass="send-button"/>
        </HBox>
    </bottom>

</BorderPane>